name: Build and Deploy Chess Bot

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      DOCKER_USERNAME: vannjok
      IMAGE_NAME: vannjok/telegram-chess-bot

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build with Maven
        run: mvn clean package -DskipTests

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: build
    runs-on: ubuntu-latest
    # ⬇️ ⬇️ ⬇️ ЗАМЕНИТЕ НА РЕАЛЬНЫЕ ДАННЫЕ! ⬇️ ⬇️ ⬇️
    env:
      SSH_HOST: "45.144.67.232"     # например: "123.123.123.123"
      SSH_USER: "root"   # например: "root" или "ubuntu"

    steps:
      - name: Check environment
        run: |
          echo "=== Deployment Configuration ==="
          echo "Server IP: $SSH_HOST"
          echo "Username: $SSH_USER"
          echo ""
          
          if [ "$SSH_HOST" = "ВАШ_IP_СЕРВЕРА" ] || [ -z "$SSH_HOST" ]; then
            echo "❌ ERROR: You need to set REAL server IP!"
            echo "1. Find your server IP: ssh to server and run 'hostname -I'"
            echo "2. Replace 'ВАШ_IP_СЕРВЕРА' in the workflow file"
            exit 1
          fi
          
          if [ "$SSH_USER" = "ВАШ_ПОЛЬЗОВАТЕЛЬ" ] || [ -z "$SSH_USER" ]; then
            echo "❌ ERROR: You need to set REAL username!"
            echo "Replace 'ВАШ_ПОЛЬЗОВАТЕЛЬ' in the workflow file"
            exit 1
          fi

      - name: Prepare SSH
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H "$SSH_HOST" >> ~/.ssh/known_hosts

      - name: Deploy to server
        run: |
          ssh "$SSH_USER@$SSH_HOST" "
            echo '🚀 Connecting to server: $(hostname)'
            echo '📁 Changing to project directory...'
          
            cd /opt/telegram-chess-bot || { echo '❌ Directory not found!'; exit 1; }
          
            echo '🔑 Logging into Docker Hub...'
            echo '${{ secrets.DOCKER_PASSWORD }}' | docker login -u '${{ env.DOCKER_USERNAME }}' --password-stdin
          
            echo '🐳 Updating Docker containers...'
            docker-compose pull
            docker-compose up -d
          
            echo '✅ Deployment completed!'
            echo '📊 Checking containers:'
            docker ps --filter 'name=telegram-chess-bot'
          "